version: 1
applications:
  - appRoot: apps/admin
    frontend:
      phases:
        preBuild:
          commands:
            - corepack enable
            - CI=true pnpm install --frozen-lockfile
            - pnpm --filter @morgan-ustd/shared build
        build:
          commands:
            - pnpm --filter @morgan-ustd/admin exec refine build
      artifacts:
        baseDirectory: apps/admin/build
        files:
          - '**/*'
      buildPath: /
